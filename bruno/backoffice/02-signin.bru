meta {
  name: Signin
  type: http
  seq: 2
}

post {
  url: http://localhost:8081/auth/signin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "photographer1@mail.com",
    "password": "password"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setVar("token", data.payload.access_token);
  bru.setVar("refresh_token", data.payload.refresh_token);
  bru.setVar("photographe_id", data.profile.id);
}
